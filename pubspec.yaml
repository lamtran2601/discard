name: flutter_discard
description: Video call.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  any_core:
    path: packages/any_core
  any_manage:
    path: packages/any_manage
  any_app:
    path: packages/any_app

  get: ^4.5.1

dev_dependencies:
  build_runner: ^2.1.5  

  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
